{
  "name": "mosaic",
  "version": "1.0.1",
  "description": "Context management for LLM applications — typed slots, lenses, transactions, token budgets",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc && tsc -p tsconfig.cjs.json",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  },
  "files": [
    "dist",
    "src"
  ],
  "license": "MIT"
}
